{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/Catalyst-Forge-LLC/ingotvault/schema/ingotvault.config.schema.json",
  "title": "ingotvault config",
  "type": "object",
  "additionalProperties": false,
  "required": ["workspaceRoot", "mirrorRoot"],
  "properties": {
    "$schema": {
      "type": "string",
      "description": "Optional JSON Schema URL for editor autocomplete."
    },
    "workspaceRoot": {
      "type": "string",
      "description": "Folder to scan for Git repositories. ~ is expanded."
    },
    "mirrorRoot": {
      "type": "string",
      "description": "Directory for bare mirror repos. ~ is expanded."
    },
    "remoteName": {
      "type": "string",
      "default": "backup",
      "description": "Git remote name to manage. Never modifies other remotes."
    },
    "maxDepth": {
      "type": "integer",
      "minimum": 1,
      "default": 3
    },
    "excludeDirNames": {
      "type": "array",
      "items": { "type": "string" },
      "default": ["__ARCHIVE", "node_modules", ".git", ".hg"],
      "description": "Directory names skipped while walking the workspace (e.g. node_modules)."
    },
    "excludeRepoGlobs": {
      "type": "array",
      "items": { "type": "string" },
      "default": [],
      "description": "Glob patterns matched against repo paths relative to workspaceRoot."
    },
    "pushAllBranches": { "type": "boolean", "default": true },
    "pushTags": { "type": "boolean", "default": true },
    "dryRun": { "type": "boolean", "default": false },
    "naming": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "style": {
          "type": "string",
          "const": "path-tree",
          "default": "path-tree",
          "description": "Preserve relative path under mirrorRoot (foo/bar → foo/bar.git)."
        },
        "prefix": { "type": "string", "default": "" },
        "suffix": { "type": "string", "default": ".git" }
      }
    },
    "safeDirectory": {
      "type": "string",
      "enum": ["per-mirror", "off"],
      "default": "per-mirror",
      "description": "per-mirror adds each mirror path to global git safe.directory (~/.gitconfig). Use off to disable."
    },
    "allowForceWithLease": {
      "type": "boolean",
      "default": false,
      "description": "If true, pushes use --force-with-lease (same as CLI --force-with-lease)."
    },
    "concurrency": {
      "type": "integer",
      "minimum": 1,
      "default": 1
    },
    "logDir": {
      "type": "string",
      "description": "Directory for --scheduled log files. ~ is expanded."
    },
    "logRetentionDays": {
      "type": "integer",
      "minimum": 0,
      "default": 30,
      "description": "Delete --scheduled logs older than this many days when writing a new log. 0 keeps forever."
    }
  }
}
