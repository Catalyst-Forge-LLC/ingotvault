{
	"name": "ingotvault-site",
	"private": true,
	"version": "0.0.1",
	"type": "module",
	"scripts": {
		"dev": "localslip claim ingotvault-site --port 5183 && filepress dev --host 0.0.0.0",
		"build": "filepress build",
		"preview": "filepress preview",
		"check": "filepress check",
		"ship": "pnpm build && wrangler pages deploy build --project-name=ingotvault",
		"deploy": "pnpm ship"
	},
	"devDependencies": {
		"getfilepress": "^0.1.39",
		"wrangler": "^4.28.0"
	},
	"pnpm": {
		"overrides": {
			"sharp": "0.35.4",
			"cookie@<0.7.0": "0.7.2",
			"js-yaml": "3.15.2"
		},
		"onlyBuiltDependencies": [
			"esbuild",
			"getfilepress",
			"workerd"
		]
	}
}
