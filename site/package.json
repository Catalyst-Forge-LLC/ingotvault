{
	"name": "ingotvault-site",
	"private": true,
	"version": "0.0.1",
	"type": "module",
	"scripts": {
		"dev": "filepress dev",
		"build": "filepress build",
		"preview": "filepress preview",
		"check": "filepress check",
		"deploy": "pnpm build && wrangler pages deploy build --project-name=ingotvault"
	},
	"devDependencies": {
		"getfilepress": "^0.1.2",
		"wrangler": "^4.28.0"
	},
	"pnpm": {
		"onlyBuiltDependencies": ["esbuild", "getfilepress", "workerd"]
	}
}
